<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only override (Gradle merges src/debug/res over src/main/res
  for assembleDebug, replacing the main/ version above) — permits cleartext
  HTTP/WS to the Android emulator's alias for the host machine (10.0.2.2) and
  localhost, so mobile:run:dev can reach a local Docker backend. A release
  build never includes this file.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
